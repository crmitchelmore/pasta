// REMOVED: HotkeyManager is no longer needed - migrated to KeyboardShortcuts library
// The global hotkey is now managed via KeyboardShortcuts.onKeyUp(for: .openPasta) in PastaApp.swift
// Shortcut recording is handled by KeyboardShortcuts.Recorder in SettingsView.swift
