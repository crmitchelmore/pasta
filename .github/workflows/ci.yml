name: CI
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  ci-gate:
    name: CI gate
    runs-on: ubuntu-latest
    steps:
      - name: Deliberate ruleset validation failure
        run: exit 1
